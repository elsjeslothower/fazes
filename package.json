{
  "name": "fazes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fazes — cycle-phase-based workout and food suggestions, buildable toward a native iOS app via Capacitor.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "preact": "^10.24.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5"
  }
}
